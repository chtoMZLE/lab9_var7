# Prompt Log: Лабораторная работа №9 (Вариант 7)

В данном файле задокументированы все итерации взаимодействия с AI-агентом (Cursor AI) в процессе разработки.

---

## Этап 1: Инициализация и архитектура
### Промпт 1
**Инструмент:** Cursor AI (Agent Mode)  
**Промпт:** > "Выполни поэтапно задачи из @work_plan.
Проект будет загружен как репозиторий, Git обязателен с осмысленной историей комитов.
Важно: мы будем работать итеративно. После каждого решенного задания ты должен написать unit - тесты.
После каждой сделанной задачи останавливайся. "

**Результат:** Агент прочитал базовый файл `work_plan` и готов к работе.

---

## Этап 2: Go — TCP-сервер и Горутины (M2, M5)
### Промпт 1
**Инструмент:** Cursor AI (Agent Mode)  
**Промпт:** > "Выполни Шаг 1 и Шаг 2 из @work_plan. Реализуй в подпапке `task2_go_tcp_server` Go TCP-сервер с простым текстовым протоколом (строка -> ответ в верхнем регистре) и используй горутины для фоновой обработки. Напиши юнит-тесты."

**Результат:** Реализован сервер и тесты. При прогоне `go test` обнаружено зависание в `NewProcessor`.

### Промпт 2 (Исправление)
**Промпт:** > "Исправь запуск горутины в `NewProcessor`: замени прямой вызов `sync.Once.Do(p.run)` на запуск в отдельной горутине `go p.run()`, чтобы не блокировать конструктор. Перезапусти тесты."

**Результат:** Баг исправлен, тесты пройдены.
- **Время:** ~25 мин.
- **Количество промптов:** 2
- **Что пришлось исправлять вручную:** ничего (исправлено через уточняющий промт)

---

## Этап 3: Rust — Модуль PyO3 (M7)
### Промпт 1
**Инструмент:** Cursor AI (Agent Mode)  
**Промпт:** > "Выполни Шаг 3: создай Rust-библиотеку в папке `rust_module`. Используй PyO3 для экспорта функции сложения чисел `fast_sum`. Настрой `Cargo.toml` и напиши Python-тест для проверки."

**Результат:** Сгенерирован `lib.rs`. Успешно собрано через `maturin develop`.
- **Время:** ~20 мин.
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** ничего

---

## Этап 4: Go — Микросервис вычислений (V1)
### Промпт 1
**Инструмент:** Cursor AI (Agent Mode)  
**Промпт:** > "Выполни Шаг 5: создай микросервис в папке `task5_go_compute_service`. Реализуй HTTP-эндпоинт `POST /primes`, считающий количество простых чисел. Напиши юнит-тесты на Go и клиент на Python."

**Результат:** Код готов. При попытке коммита замечены лишние файлы `.pyc`.

### Промпт 2 (Оптимизация)
**Промпт:** > "Добавь `__pycache__` и `*.pyc` в `.gitignore`, удали их из индекса Git и сделай коммит."

**Результат:** Репозиторий очищен, функционал работает.
- **Время:** ~20 мин.
- **Количество промптов:** 2
- **Что пришлось исправлять вручную:** ничего (исправлено через уточняющий промт)

---

## Этап 5: CI/CD для Rust (V3)
### Промпт 1
**Инструмент:** Cursor AI (Agent Mode)  
**Промпт:** > "Выполни Шаг 4: настрой GitHub Actions для автоматической сборки Rust-модуля через `maturin-action` и публикации в PyPI при создании тега."

**Результат:** Создан `.github/workflows/release.yml` с мультиплатформенной сборкой.
- **Время:** ~10 мин.
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** ничего 

---

## Итоговая статистика
| Показатель | Значение |
| :--- | :--- |
| **Всего промптов** | 7 |
| **Ручные правки** | Минимальные (установка зависимостей) |
| **Общее время** | ~75 минут |
