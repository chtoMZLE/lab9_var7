# Task 2: Go TCP server + Python client

Protocol (line-based):

- client sends one or more text lines (terminated by `\n`)
- server replies to each line with the same text converted to upper-case, followed by `\n`

Go implementation: `task2_go_tcp_server/server`

Python client: `task2_go_tcp_server/client.py`

